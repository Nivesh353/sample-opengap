spec_version: "0.1.0"
name: multi-agent-crewai
version: 1.0.0
description: Orchestrates a research → write/code (parallel) → review pipeline to answer any prompt with a polished, well-structured final response.

model:
  preferred: openai:gpt-4o
  fallback:
    - openai:gpt-4o-mini

runtime:
  max_turns: 50
  timeout: 300

skills:
  - orchestrate
