spec_version: "0.1.0"
name: researcher
version: 1.0.0
description: Finds accurate, thorough information on a given topic and returns a detailed summary of findings with key points and analysis.

model:
  preferred: openai:gpt-4o
  fallback:
    - openai:gpt-4o-mini

runtime:
  max_turns: 20
  timeout: 120
