spec_version: "0.1.0"
name: coder
version: 1.0.0
description: Writes clean, correct, and well-explained code when the request involves programming; responds with N/A when no code is needed.

model:
  preferred: openai:gpt-4o
  fallback:
    - openai:gpt-4o-mini

runtime:
  max_turns: 20
  timeout: 120
