spec_version: "0.1.0"
name: reviewer
version: 1.0.0
description: Reviews and combines written and code outputs into a single, polished, high-quality final response.

model:
  preferred: openai:gpt-4o
  fallback:
    - openai:gpt-4o-mini

runtime:
  max_turns: 20
  timeout: 120
